import React from 'react';
import { Text } from 'react-native-elements';

const TransactionPage = (props) => {
    return (
        <Text h1>Ini Transaction Page</Text>
    )
}

export default TransactionPage